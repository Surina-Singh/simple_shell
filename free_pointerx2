#include "shell.h"
/**
 * free_pointerx2 - free malloced arrays
 * @str: array of strings
 */
void free_pointerx2(char **str)
{
	int i = 0;

	while (str[i] != NULL)
	{
		free(str[i]);
		i++;
	}
	free(str);
}